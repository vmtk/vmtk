set(TEST_PYPES_SRCS
  test_pype.py
  test_pypes.py
  )

IF(NOT TEST_PYPES_INSTALL_LIB_DIR)
  SET(TEST_PYPES_INSTALL_LIB_DIR tests/test_PypeS)
ENDIF(NOT TEST_PYPES_INSTALL_LIB_DIR)

INSTALL(FILES ${TEST_PYPES_SRCS}
  DESTINATION ${TEST_PYPES_INSTALL_LIB_DIR}
  COMPONENT PythonRuntimeLibraries)